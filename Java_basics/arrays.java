// Different ways to declare
int[] myNumbers;  // preferred way
int myNumbers[];  // alternative (less common)

// Method 1: Create and initialize separately
int[] numbers = new int[3];
numbers[0] = 10;
numbers[1] = 20;
numbers[2] = 30;

// Method 2: Create and initialize at once
int[] numbers = {10, 20, 30};